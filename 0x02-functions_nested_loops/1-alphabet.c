#include "main.h"
/**
  * main - Entry Point
  * description using _putchar to print out the alphabet"
  * Return: always 0 (success)
  */
void print_alphabet(void);

int main(void)
{
	char i;

	for (i = 97; i <= 122; i++)
	{
		_putchar('i' + 0);
	}
	_putchar('\n');
	return (0);
}
