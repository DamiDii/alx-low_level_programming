#include "main.h"
/**
  *main -entry point
  * description using _putchar to print out the alphabet"
  * Return: always 0 (success)
  */
int main(void)
{
	int i;

	for (i = 97; i <= 122, i++)
	{
		_putchar('i');
	}
	return (0);
}
