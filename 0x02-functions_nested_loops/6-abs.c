#include "main.h"
/**
  * _abs - get the absolute value of an intger
  * @n: the int to check
  * Return: the absolute integer
  */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}

