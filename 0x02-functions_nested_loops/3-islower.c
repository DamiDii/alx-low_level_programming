#include "main.h"
/**
  * main - Entry Point
  * description using _putchar to print out the alphabet"
  * @c: is thr char to  be char
  * Return: always 0 (success)
  */
int _islower(int c);

int main(void)
{
	if (c >= 'a' && c <= 'z')
		return (1);
	else
		return (0);
}
